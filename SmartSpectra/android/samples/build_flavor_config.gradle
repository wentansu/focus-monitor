android {
    buildFeatures {
        flavorDimensions += "sdk"
    }
    productFlavors {
        register("internal") {
            dimension = "sdk"
        }
        register("external") {
            dimension = "sdk"
        }
    }
}

dependencies {
    internalImplementation(project(":sdk"))
    externalImplementation("com.presagetech:smartspectra:1.0.25")
}
